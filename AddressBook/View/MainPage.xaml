<Page
    x:Class="AddressBook.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AddressBook"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:AddressBook.Model"
    xmlns:viewModel="using:AddressBook.ViewModel"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Name="PersonListBind" x:DataType="model:Person">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding FirstName}"></TextBlock>
                <TextBlock Text="{Binding LastName}"></TextBlock>
                <TextBlock>TEST</TextBlock>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    

    
    <SplitView>
        <SplitView.Pane>
            <ListView ItemTemplate="{StaticResource PersonListBind}" BorderThickness="0,2,0,1" BorderBrush="Gray" ItemsSource="{Binding Persons}" SelectedItem="{Binding SelectedPerson, Mode=TwoWay}" />
        </SplitView.Pane>
    </SplitView>

</Page>
